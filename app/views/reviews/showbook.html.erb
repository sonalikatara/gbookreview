<% r1 = @reviews.first %>

<p>
   <b> Title : </b> <%= r1.title %>
</p>
<p>
   <b> Author : </b> <%= r1.author %>
</p>
<p>
     <%= image_tag( r1.bookcover.url(:small), :alt=> r1.title)%> 

</p>
<div class="apple_pagination">
<%= will_paginate @reviews, :container => false %> 
</div>
<% @reviews.each do |r| %>
<p>
   <b> Reader : </b> <%= link_to r.reader.readerName, show_reader_path(:reader_id => r.reader)%> 

   <br><b> Review : </b> <%=  render 'reviews/shared_files/display_review_rating', rating: r.rating %>
   <br> <%= r.comment %>
</p>
<% end %>
<div class="apple_pagination">
<%= will_paginate @reviews, :container => false %> 
</div>

